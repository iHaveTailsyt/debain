# Install sudo. Think of it as the Linux version of Windows Administrator user.
apt install sudo

# Update/Upgrade the packages
sudo apt update
sudo apt upgrade -y

# Fix missing packages.
sudo apt update --fix-missing

# Install apt-utils
sudo apt install apt-utils
clear